spring:
  application:
    name: warehouseflow-backend

  datasource:
    url: jdbc:postgresql://localhost:5432/warehouseflow
    username: postgres
    password: gioele91@
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080

app:
  jwtSecret: "warehouseflowsupersecretkey123456789"
  jwtExpirationMs: 86400000
  corsAllowedOrigins: "http://localhost:5173"
